
# Define the seeds to inject into the target stores
# Supported providers are sql (data only)
# Only gzipped tarballs are supported as the storage format

---
# Defines an ordered array of sql data seeds/scripts to be restored in array order into a target
# - 
#   seed: demo-belgium-latest
#   target: 
#     host: data-broker-service-db
#     port: 5432
#     database: data_modules
#   data:
#     provider: aws
#     bucket: snd-integration-tests-data
#     path: seeds/demo-belgium-latest.tar.gz

# - 
#   seed: mobile
#   target: 
#     host: data-broker-service-db
#     port: 5432
#     database: data_modules
#   data:
#     provider: minio # Support aws / minio / local (in seeds folder)
#     bucket: batches-data-seeds
#     path: ria/qa/qa-dim.20190609133701.tar.gz

# - 
#   seed: local
#   target: 
#     host: data-broker-service-db
#     port: 5432
#     database: data_modules
#   data:
#     provider: local # Support aws / minio / local (in seeds folder)
#     path: seeds/sql/local
